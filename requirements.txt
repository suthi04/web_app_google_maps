# ===== InsightReview — core dependencies (DEMO MODE) =====
# ติดตั้งชุดนี้ก็รันโหมดเดโมได้ทันที (ใช้ข้อมูลตัวอย่าง + วิเคราะห์อารมณ์แบบ lexicon)
#   pip install -r requirements.txt

Flask==3.1.3
requests==2.33.1
pythainlp==5.3.4
anthropic>=0.40    # optional — only needed for the Claude (LLM) extraction engine
